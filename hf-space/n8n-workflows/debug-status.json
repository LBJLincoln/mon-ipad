{
  "name": "Debug Status",
  "active": true,
  "nodes": [
    {
      "parameters": {
        "path": "debug-status",
        "httpMethod": "={{$parameter.httpMethod}}",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300],
      "id": "debug-webhook",
      "name": "Debug Webhook",
      "webhookId": "debug-status"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ status: 'ok', version: 'v3', timestamp: new Date().toISOString(), env: { JINA: $env.JINA_API_KEY ? 'SET' : 'UNSET', PINECONE: $env.PINECONE_API_KEY ? 'SET' : 'UNSET', SUPABASE: $env.SUPABASE_PASSWORD ? 'SET' : 'UNSET', OPENROUTER: $env.OPENROUTER_API_KEY ? 'SET' : 'UNSET', NEO4J: $env.NEO4J_AUTH ? 'SET' : 'UNSET' } }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [470, 300],
      "id": "debug-respond",
      "name": "Respond"
    }
  ],
  "connections": {
    "Debug Webhook": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
