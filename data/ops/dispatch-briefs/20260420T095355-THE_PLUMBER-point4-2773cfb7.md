# THE_PLUMBER — Point #4: Prompt-byte menu visibility unit test (ITF crypto-pass RCA lesson)

_Dispatched 2026-04-20T09:53:55Z — severity=3_

**Department:** D6 Evaluation
**Target file(s):** `scripts/arena/hf-intraday-trading-floor/test_build_prompt.py (NEW)`

## Why This
ITF 84% crypto-pass was caused by _build_prompt slicing quotes[:22] — 48 equities filled slot, crypto invisible despite prompt text saying 'bet crypto'. Bug would recur.

## Spec (concrete steps)
1. New test_build_prompt.py asserts: for each persona, _build_prompt output contains ≥1 string from EACH asset class (equity/leveraged/crypto/option)
2. Test with synthetic quotes (71 instruments across classes) to match prod universe shape
3. Fail test if any class absent from menu — protects against future truncation
4. Also verify CRYPTO_PIVOT_CLAUSE literal substring present when crypto |change_pct|>0.2%
5. Wire into GH Action .github/workflows/itf-ci.yml (NEW) — run on every PR touching scripts/arena/hf-intraday-trading-floor/
6. Extend same pattern to NBA _build_prompt (verify pp_* visible when unlocked), POL _build_prompt (verify ≥2 cats)

## Acceptance Criteria
Failing the crypto class -> red CI; passing 7 personas × 4 classes = 28 green

## Context
- Full empire ledger: `data/empire/MASTER.md`
- Your per-agent brief: `data/empire/briefs/the_plumber.md`
- Dispatch-log: `data/ops/dispatch-log.jsonl`
- Live 3-min intel: `data/ops/tf-intel-latest.json`

## How to Ack
When you start: `git log --author="THE_PLUMBER"` should show your first commit within 24h.
When done: update `data/empire/strategy-scorecard.json` point-4 status → DONE.
