# LAUNCHPAD — Point #9: Retire keepalive-spaces.sh for TF Spaces (replaced by tf_intel_monitor)

_Dispatched 2026-04-20T09:53:55Z — severity=2_

**Department:** D9 Cross-Repo
**Target file(s):** `crontab + scripts/keepalive-spaces.sh + scripts/ops/tf_intel_monitor.py`

## Why This
tf_intel_monitor runs every 3 min and detects per-agent silence — far more precise than every-30-min binary ping. Keepalive is noise for TF Spaces.

## Spec (concrete steps)
1. Read scripts/keepalive-spaces.sh → list which Spaces it pings
2. Split into 2 lists: TF_SPACES (NBA/POL/PQTF/ITF — owned by tf_intel_monitor) vs OTHER (depts, pixel-world, langfuse, gateway, islands)
3. Create scripts/keepalive-non-tf.sh that only pings OTHER list
4. Update crontab: replace keepalive-spaces.sh with keepalive-non-tf.sh (same 30-min cadence)
5. Keep old script for rollback but stop invoking via cron
6. Verify tf_intel_monitor covers all 4 TF Spaces in its checks (grep for each URL in scripts/ops/tf_intel_monitor.py)

## Acceptance Criteria
crontab no longer invokes keepalive-spaces.sh; tf-intel-summary.md shows no gap

## Context
- Full empire ledger: `data/empire/MASTER.md`
- Your per-agent brief: `data/empire/briefs/launchpad.md`
- Dispatch-log: `data/ops/dispatch-log.jsonl`
- Live 3-min intel: `data/ops/tf-intel-latest.json`

## How to Ack
When you start: `git log --author="LAUNCHPAD"` should show your first commit within 24h.
When done: update `data/empire/strategy-scorecard.json` point-9 status → DONE.
