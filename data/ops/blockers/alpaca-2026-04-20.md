# Alpaca credentials blocker — 2026-04-20

## Status
**Partially live** — HF Space `LBJLincoln26/intraday-trading-floor` has valid keys set
(50% of orders succeed, confirmed by user dashboard activity). The 50% 401s need
rate-limit diagnosis, NOT a credential swap.

## Credentials user provided this session (non-usable for our ITF executor)

1. **OAuth App #1** — `CKIIM5G6PR5GIQQNEKGZJAPC7Z` / `8X2JEVza5h6KGhUoHFGcEEWVCAoQHxHdnqF1SCoMTZAF`
   Tested `paper-api.alpaca.markets/v2/account` + `api.alpaca.markets/v2/account` → 401 on both.
2. **Broker sandbox app** — `CKQJHKFYUDRBER5RZTGHGKVAVG` / `6T2aXgNNNE8uFfzu8KB5uyDbiRPrgjZnNzHm3X2YEM5Y`
   Base URL `https://broker-api.sandbox.alpaca.markets`. Tested 4 auth patterns
   (`-u` basic, explicit base64 Authorization, `APCA-API-KEY-ID` headers,
   trailing-slash variant) → all 401.
3. **2FA device ID** — UUID `9db53155-fa7c-4c9d-8fb1-b672ad7bae1f` (not an API key)

## Why they don't work with our executor
`scripts/arena/hf-intraday-trading-floor/executor.py:99` calls
`https://paper-api.alpaca.markets/v2/orders` with headers
`APCA-API-KEY-ID` / `APCA-API-SECRET-KEY`. That endpoint only accepts
**Trading API Keys** (format `PKxxxxxxxxxxxxxxxxxx` paper or `AKxxxxxxxxxxxxxxxxxx`
live, 20 chars + secret ~40 chars base64). OAuth App Client IDs (`CKxxxxx...`)
and Broker API sandbox creds go to different endpoints with different auth
flows (token exchange or basic auth over `broker-api.*`).

## What's currently live (needs investigation, not replacement)
HF Space already has `ALPACA_PAPER_KEY` + `ALPACA_PAPER_SECRET` secrets set.
50% throughput = they work but something else throttles the other 50%.
Most likely causes (ranked):
1. Rate limit — Alpaca paper = 200 req/min. ITF burst may spike over.
2. Partial key rotation — one key pair valid, another revoked.
3. Specific endpoint auth issue — `/v2/orders` vs `/v2/positions`.

## What we need to unblock fully
From https://app.alpaca.markets/paper/dashboard/overview → sidebar right → "API Keys" → "Generate New Key":
- `APCA-API-KEY-ID` (format `PK...`, 20 chars)
- `APCA-API-SECRET-KEY` (format `<base64>`, ~40 chars, shown ONCE)

## Action queued for LAUNCHPAD
Don't swap keys. Investigate the 50% 401 rate via Alpaca response headers
(`x-ratelimit-remaining`, `x-ratelimit-reset`) and the executor's request
cadence.
