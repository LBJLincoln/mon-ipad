# DAY-0 RESET — 2026-04-25 20:53Z

User authorized: "ok, sodo all perfect scientific" after seeing structural fixes confirmed.

## Scope
- **NBA** ✅ wiped — HF commit `44cf6715b5d5` deleted 113 files (111 decisions + state.json + council_plans.json)
- **ITF** ✅ wiped — HF commit `b414de1ba4d3` deleted 4 intraday files (agent_bankrolls.json + agent_ledger.jsonl + positions.json + fill_reconciliation_cursor.json)
- **POL** ❌ EXCLUDED — actively compounding +14.5% alpha at day-154. llama-contra +$160, qwen-quant +$202, mistral-small +$140. Resetting would wipe validated structural-divergence + Kelly-tune doctrine. Per `feedback_carte_blanche_aggressive_apr22.md` — confirm before POL wipes.
- **PQTF** ❌ FROZEN FOREVER — $602K validation artifact, CLAUDE.md rule 12.

## Pre-flight
- Champion-preserve scan ran: 0 captured this run (no agent ≥ $500 threshold), 22 total historically preserved.
- Quarantine status: NBA + POL "expired" (window passed), ITF not in quarantine.
- Both Spaces: `factory_reboot=True` triggered.

## Fixes carried into Day 0 baseline

All structural fixes from this session deployed BEFORE the reset:

### NBA (`scripts/arena/hf-llm-trading-floor/app.py`)
1. `NBA_HIDE_PP=1` (default) — pp_* hidden from prompt menu AND blocked at parser entry. LLMs hallucinate pp_* names from training data; this kills it at both layers.
2. **Engine-edge override** at parse — replaces LLM-reported `edge` with `model_preds[game_key].per_category[cat].edge`. **Critical key-format fix**: was looking up `"AWAY@HOME"`, engine actually keys `"DATE_AWAY@HOME"` (line ~4182). Lookup now tries date-prefixed first, falls back to substring match.
3. `engine_forced_floor` (parser + day-level) — anti-groupthink-cascade. When LLM emits 0 allocations, inject the top engine-validated edge as a small forced bet (pct=0.02). Day-level pass at `day_log` assembly catches agents whose parse_day_allocation never ran (NO_LLM_RESPONSE / PARSER_DROPPED).
4. **Settlement-gate bypass** for engine-validated sources — `engine`, `engine_forced_floor`, `llm_fallback_singleton`, `engine_fallback_singleton`, `llm_capped` all bypass tier `MIN_EDGE` (0.09 for bk<$100). Without this, engine-validated bets at edge ~0.06 silently dropped.
5. **Bankroll circuit breaker** — `NBA_BANKROLL_FLOOR_USD=$30` (default). Forces PASS when below. Applied AFTER silent-pass branch so blocks fabricated uniform-fallback fabrications too.
6. **Edge-hallucination guard** — when ≥3 allocations share same 3-decimal-rounded edge, drop everything beyond first 2.
7. **Walk-forward depth banner** in prompt — "10+ NBA seasons walk-forward, 19-week CV, 7,213 features, 249 odds cats". Engine edges sorted by `|edge|` desc, tagged with reliability tier `[HIGH|MEDIUM|LOW]` per category-class.
8. **`_ff_diag` telemetry** per agent — every gate decision logged into day_log for forensic.

### ITF (`scripts/arena/hf-intraday-trading-floor/executor.py`)
1. **`ITF_PREFER_NON_BRACKET=1`** (Space env) — equity orders use simple market+notional (regt BP) not bracket+DTBP. Survives PDT exhaustion.
2. **`cross_agent_collision_skip`** — submit() pre-check: if any other agent has opposite-side open on same ticker, skip clean.
3. **`fleet_dir_cap_skip`** — max 3 agents per (ticker, side). Tunable `ITF_MAX_AGENTS_PER_DIR_TICKER`. Forces fleet diversification.
4. **Integer-qty non-bracket for shorts** — Alpaca rejects fractional shorts; force int_qty. ValueError surfaced as helpful broker_error.

### POL (carried — fleet still running)
- `_AGENT_KELLY_OVERRIDE`: gemini-tact 0.13, qwen-arb 0.13, gemini-anl 0.10 explicit, llama-contra 0.18, qwen-quant 0.18.

### Vercel `/audit` page
- Live at `nomosdashboard.vercel.app/audit`. Coverage / per-agent-deep / per-game-deep tabs all serving 200.
- Build error fixed: `AGENTS Record<Exclude<Tab, 'diagnostic' | 'coverage'>, string[]>`.

## Day 0 expectations

For NBA:
- Day-000 should write within 3-5 min after rebuild
- All 17 agents seeded $100, fleet $1700
- Bets/day target: 11-13 fleet-wide (some LLM provider variance)
- Source mix: `engine` / `engine_forced_floor` / `llm_fallback_singleton` / `llm_capped` (no `pp_*`, no hardcoded 0.111 edges)
- Circuit breaker dormant (no bk yet below $30)
- Compound trajectory should outperform the pre-reset -55% drawdown

For ITF:
- Per-tick `dec=17, trades>=10` typical
- 17 agents fresh-seeded ~$5,857 each
- broker_error mix: minimal cross_agent_collision_skip + fleet_dir_cap_skip from divert pool
- No more bracket-DTBP errors (40310000 with cost_basis)

## Monitor cadence

30-min intervals via ScheduleWakeup. Verify Day 0+1 produces non-empty day file with engine-source-dominant src distribution.
