{
  "plan_id": "plan-20260311-213944",
  "target_sector": "finance",
  "target_pipeline": "quantitative",
  "category": "fix_pipeline",
  "actions": [
    {
      "step": 1,
      "type": "eval",
      "description": "Run targeted quantitative pipeline test to confirm timeout error",
      "command": "python3 eval/parallel-eval.py --sector finance --pipeline quantitative --single_test",
      "reason": "Verify the HTTP 0 timeout error is reproducible and isolate the exact failure point"
    },
    {
      "step": 2,
      "type": "script",
      "description": "Analyze LiteLLM configuration and SQL generation timeout",
      "command": "python3 ops/metrics-collector.py --pipeline quantitative --profile --timeout_analysis",
      "reason": "Identify if the timeout occurs during LiteLLM call or SQL generation step"
    },
    {
      "step": 3,
      "type": "n8n_change",
      "description": "Apply FIX-87 from debug playbook - increase LiteLLM timeout and optimize SQL generation",
      "command": "Update quantitative pipeline node: Set LiteLLM timeout to 60s, optimize SQL query generation parameters",
      "reason": "FIX-87 specifically addresses HTTP timeout errors in quantitative pipelines by adjusting timeout thresholds"
    },
    {
      "step": 4,
      "type": "eval",
      "description": "Test quantitative pipeline with sample finance questions",
      "command": "python3 eval/parallel-eval.py --sector finance --pipeline quantitative --test_questions \"What is the IFRS 16 lease accounting treatment?\" \"Calculate EBITDA from financial statements\"",
      "reason": "Verify the pipeline now returns valid responses instead of timing out"
    },
    {
      "step": 5,
      "type": "eval",
      "description": "Run full parallel evaluation to measure impact on overall scores",
      "command": "python3 eval/parallel-eval.py --sector finance --pipeline quantitative --full_eval",
      "reason": "Confirm the fix improved quantitative pipeline accuracy from 0% to meaningful score"
    }
  ],
  "test_questions": [
    {
      "question": "What is the IFRS 16 lease accounting treatment for operating leases?",
      "sector": "finance",
      "expected_behavior": "Returns detailed explanation of IFRS 16 requirements with proper terminology and source citations"
    },
    {
      "question": "Calculate EBITDA from the following financial data: Revenue 1M, COGS 600K, SG&A 200K, Depreciation 50K",
      "sector": "finance",
      "expected_behavior": "Returns correct EBITDA calculation (250K) with step-by-step explanation and financial terminology"
    }
  ],
  "success_metric": "Quantitative pipeline accuracy score > 0 (currently 0%) and no HTTP timeout errors",
  "estimated_duration_min": 45,
  "risk_assessment": "Risk of breaking other pipeline components if timeout changes affect LiteLLM configuration globally. Need to test standard and graph pipelines after fix."
}