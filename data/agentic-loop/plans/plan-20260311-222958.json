{
  "plan_id": "plan-20260311-222958",
  "target_sector": "btp",
  "target_pipeline": "quantitative",
  "category": "fix_pipeline",
  "actions": [
    {
      "step": 1,
      "type": "eval",
      "description": "Run targeted quantitative pipeline test to capture exact error details",
      "command": "python3 eval/parallel-eval.py --sector btp --pipeline quantitative --questions 5 --debug",
      "reason": "Need to capture the full error response and HTTP details to understand the timeout cause"
    },
    {
      "step": 2,
      "type": "script",
      "description": "Analyze LiteLLM configuration and SQL generation logic",
      "command": "python3 ops/metrics-collector.py --pipeline quantitative --profile --output sql_analysis.json",
      "reason": "The timeout likely stems from SQL generation or LiteLLM configuration issues that need deep analysis"
    },
    {
      "step": 3,
      "type": "n8n_change",
      "description": "Check LiteLLM timeout settings and increase if needed",
      "command": "Modify quantitative pipeline LiteLLM node timeout from default (30s) to 60s",
      "reason": "Quantitative queries may require more processing time for complex calculations"
    },
    {
      "step": 4,
      "type": "n8n_change",
      "description": "Add error handling and retry logic for HTTP timeouts",
      "command": "Add error catch node and retry mechanism for HTTP 0 timeout errors",
      "reason": "Prevent complete pipeline failure and allow automatic recovery from transient timeouts"
    },
    {
      "step": 5,
      "type": "eval",
      "description": "Verify fix with comprehensive quantitative test",
      "command": "python3 eval/parallel-eval.py --sector btp --pipeline quantitative --questions 20 --validate",
      "reason": "Confirm the pipeline now returns valid responses and achieves baseline accuracy"
    }
  ],
  "test_questions": [
    {
      "question": "Calculate the load-bearing capacity of a reinforced concrete beam with dimensions 300mm x 500mm, using concrete class C30/37 and steel B500C, with a span of 6 meters",
      "sector": "btp",
      "expected_behavior": "Returns a numerical calculation with proper Eurocode formulas cited, including concrete strength, steel yield, and moment calculations"
    },
    {
      "question": "What is the thermal resistance (R-value) of a wall assembly consisting of 100mm concrete block, 50mm insulation, and 12mm plasterboard?",
      "sector": "btp",
      "expected_behavior": "Provides R-value calculation with thermal conductivity values cited from RE2020 or relevant thermal standards"
    }
  ],
  "success_metric": "BTP quantitative pipeline score > 0% (baseline functionality restored), with no HTTP 0 timeout errors",
  "estimated_duration_min": 45,
  "risk_assessment": "Increasing timeout may cause longer pipeline execution times; retry logic could mask underlying performance issues if not monitored"
}