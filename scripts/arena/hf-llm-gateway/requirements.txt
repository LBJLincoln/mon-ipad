gradio>=5.0
requests
fastapi
uvicorn
pydantic>=2.0
# Logfire — Pydantic's OTel observability SDK (10M spans/mo free)
# Auto-instruments requests + fastapi; single write-token env var LOGFIRE_TOKEN
logfire>=2.0
