# Nomos42 LLM Space — HF Spaces free CPU tier
# Python 3.10+ required

# Core: GGUF model runner
# Prebuilt CPU wheel — avoids C++ compilation on HF Spaces (30+ min build)
# Using extra-index-url with prebuilt wheel from abetlen/llama-cpp-python releases
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.2

# HuggingFace Hub — for model download
huggingface_hub>=0.23.0

# Web framework
gradio>=5.9.0
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
