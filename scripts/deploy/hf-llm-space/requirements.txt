# Nomos42 LLM Space — HF Spaces free CPU tier
# Python 3.10+ required

# Core: GGUF model runner
# Prebuilt CPU wheels from abetlen — no compilation required on HF Spaces
llama-cpp-python==0.3.9

# HuggingFace Hub — for model download
huggingface_hub>=0.23.0

# Web framework
gradio>=4.44.0
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
