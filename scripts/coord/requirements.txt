requests>=2.31
# Optional — install on VM to upgrade the plain-Python tick to a LangGraph state machine.
# Without these, coord.py still runs the same 4 nodes but without durable graph state.
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
