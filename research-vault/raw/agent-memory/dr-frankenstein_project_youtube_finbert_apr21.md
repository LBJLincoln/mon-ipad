---
name: YouTube FinBERT sentiment features shipped (Cat 67 NBA / Cat 44 POL) + OAuth subscriptions
description: HAWKEYE 2026-04-21 proposal executed — 3 workstreams (channel seeds, FinBERT rolling, OAuth) in one cycle
type: project
---

Shipped 2026-04-21 per HAWKEYE youtube-to-training-features proposal. 3 linked
workstreams in one FRANKENSTEIN cycle.

**W1 — NBA+POL channel seeds (commit b74c6bb81, mon-ipad)**
Added 7 NBA + 5 POL channels to `scripts/youtube_channel_autofetch.py` CHANNELS
dict. Resolved via `forHandle` API with `search.list` fallback for @ESPNNBA,
@AxiosHQ, @VoxDotCom, @RealClearPolitics (forHandle misses). Post-autofetch run:
246 new videos across 34 channels, **NBA keyword-match 4.9%→20.7% (97/469)** —
unblocks HAWKEYE Tier-3 NBA co-mention features.

**W2 — FinBERT rolling sentiment (commits e1120d98b mon-ipad + fbdb750 POL + ecacc01ef tests)**
- NBA engine: Cat 67 `yt_pol_mean_{3,7,14}` + `yt_abs_pol_mean_{3,7,14}`.
  `ENGINE_VERSION = v3.2-67cat`. `enable_youtube=False` default (corpus immature,
  flip True on S14 sandbox for A/B).
- POL engine: Cat 44 `yt44_*` parallel. `ENGINE_VERSION = v3.22-political-44cat-youtube-finbert-rolling`.
  Registered in categories_ext via `enable_youtube_finbert=True` default — all-zero
  fallback when parquet missing, safe to ship pre-populate.
- Loader reads `data/youtube/sentiment.parquet` (regenerated by
  `scripts/youtube_sentiment_precompute.py` via `ProsusAI/finBERT`, already
  committed at e1120d98b). Parquet is **gitignored** (regenerable artifact).
- Tests: `scripts/test_youtube_sentiment_features.py`, 9 cases all green.
  Locked behaviours: engine version, feature names, 3/7/14d rolling math,
  sim_date_cutoff leakage gate, post-cutoff-drop ratio matches HAWKEYE 152/229
  audit shape, POL Cat 44 parity.
- SHA parity: `features/engine.py` == `hf-space/features/engine.py` both repos.

**W3 — OAuth 2.0 subscriptions puller (shipped in commit e1120d98b)**
`scripts/youtube_oauth_subscriptions.py` — 2-step flow:
1. Bare run prints auth URL (OOB redirect, `youtube.readonly` scope).
2. Re-run with `--auth-code <code>` → exchanges for refresh token at
   `data/credentials/youtube-oauth-refresh.json` (gitignored).
3. Subsequent runs: silent refresh → pull subs + liked + own uploads → merge
   into CHANNELS dict + manual-ingested.json (dedupe by video_id).
Deps: `google-auth-oauthlib`, `googleapiclient` — errors explicit if missing.
**Status: blocked on user paste** (expected per proposal). Auth URL already
printed; user needs to complete OAuth in browser once.

**Expected Brier delta (POL)**: +0.0015 to +0.0030 (Yang MDPI 2025 anchor, SHAP
top-10 evidence). Kill criterion: Δ<+0.0005 after 200 gens on POL P5/P7 → revert.

**Commit SHAs (reference)**:
- mon-ipad: b74c6bb81 (W1 seeds) → e1120d98b (W2 engine+W3 OAuth) → ecacc01ef (W2 tests)
- nomos-political-alpha: fbdb750 (POL Cat 44)

Files:
- `/home/termius/mon-ipad/scripts/youtube_channel_autofetch.py` (+12 channels)
- `/home/termius/mon-ipad/scripts/youtube_sentiment_precompute.py` (FinBERT precompute, idempotent)
- `/home/termius/mon-ipad/scripts/youtube_oauth_subscriptions.py` (OAuth puller)
- `/home/termius/mon-ipad/scripts/test_youtube_sentiment_features.py` (9 tests)
- `/home/termius/mon-ipad/features/engine.py` (+ hf-space mirror, v3.2-67cat)
- `/home/termius/nomos-political-alpha/features/political_engine.py` (+ hf-space mirror, v3.22-political-44cat)
